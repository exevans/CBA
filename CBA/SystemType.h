#ifndef SYSTEM_TYPE_H
#define SYSTEM_TYPE_H

enum class SystemType { Logic=1, Graphics, Input, Animation, Interact, Dialogue, Collision, NavMesh, ScriptState, Movement, Schedule, Timer, Lighting, Count };

#endif